---
name: refactor
description: Workflow рефакторинга — анализ → рефакторинг → проверка → документация. Senior-reviewer ищет цели, refactor применяет изменения, test-runner подтверждает отсутствие смены поведения.
---

# Команда Refactor

## ⛔ ЗАПРЕЩЕНО ВЫПОЛНЯТЬ РАБОТУ САМОСТОЯТЕЛЬНО

**Не анализируй код сам. Не рефакторь файлы. Не запускай тесты напрямую.**

Каждый шаг выполняется только субагентом через инструмент `Task`.
Ты только координатор. Если собираешься сделать что-то кроме вызова `Task` — остановись.

---

## ОБЯЗАТЕЛЬНО: прочитай skill и следуй ему

1. Прочитай `.cursor/skills/refactor-workflow/SKILL.md` инструментом Read — сразу, до любых других действий.
2. Выполняй **точно** по skill — через `Task(subagent_type=...)` на каждом шаге.
3. Не пропускай, не сокращай и не упрощай шаги из skill.
