---
name: audit
description: Полный аудит здоровья проекта — архитектура → безопасность → качество кода → отчёт → (опционально) исправления. Сводный отчёт; при критических находках — предложение запустить подходящий workflow исправлений в том же чате.
---

# Команда Audit

## ⛔ ЗАПРЕЩЕНО ВЫПОЛНЯТЬ РАБОТУ САМОСТОЯТЕЛЬНО

**Не анализируй код сам. Не пиши отчёты. Не редактируй файлы. Не запускай инструменты напрямую.**

Каждый шаг выполняется только субагентом через инструмент `Task`.
Ты только координатор. Если собираешься сделать что-то кроме вызова `Task` — остановись.

---

## ОБЯЗАТЕЛЬНО: прочитай skill и следуй ему

1. Прочитай `.cursor/skills/audit-workflow/SKILL.md` инструментом Read — сразу, до любых других действий.
2. Выполняй **точно** по skill — через `Task(subagent_type=...)` на каждом шаге.
3. Не пропускай, не сокращай и не упрощай шаги из skill.
